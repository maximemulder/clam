type Tuple = [T] (Int, T);
type Make = [T] -> (T) -> (Int, T);

def make_zero: Make = [T] => (p: T) -> Tuple[T] =>
    @(0, p);

def main = {
    var a: Tuple[String] = make_zero[String]("Hello");

    print(a);

    print(a.0);
    print(a.1);
};
