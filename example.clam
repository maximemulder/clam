def j = (p) ->
    print(p);
    j

def main =
    j("Hello")("World")("!");
    unit

def gen = (i, u) ->
    {i, gen(i + 1)}

def rec = (gen) ->
    var pair = gen(unit);
    print(pair.0);
    rec(pair.1)

type List = [T] => { element: T, next: List[T] } | Unit

// def main = rec(gen(0))
