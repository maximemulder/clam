def j = (p) ->
    print(p);
    j

def main =
    j("Hello")("World")("!");
    unit

def gen = (i, u) ->
    var i = i + 1;
    {i = i, gen = gen(i)}

def rec = (gen) ->
    var pair = gen(unit);
    print(pair.i);
    rec(pair.gen)

def main = rec(gen(0))
