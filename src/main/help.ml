let help =
"Description:

    The Clam CLI. Use this program to interpret a Clam file or enter the Clam
    REPL.

Usage:

    clam [--options] <file>

Options:

    -h --help          Display this message.
    -i --interactive   Open the Clam REPL (read-eval-print loop). If this option
                       is present, the interpreted file is optional.
    -t --types         Display the type of all definitions.
    -v --values        Evaluate and display the results of all definitions. If
                       this option is not present, only the 'main' definition is
                       evaluated.
"
